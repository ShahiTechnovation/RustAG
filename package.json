{
  "name": "rustag",
  "version": "0.1.0",
  "private": true,
  "description": "RustAG — Pre-execution assurance for Robinhood Chain. Rehearse privileged transactions, inspect state changes, detect unexpected behavior, and generate verifiable evidence before signing.",
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev",
    "dashboard": "pnpm --filter dashboard dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
