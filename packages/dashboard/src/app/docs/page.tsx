import Link from 'next/link';
export default function Docs() { return <div className="workspace docs"><div className="page-heading"><div><div className="eyebrow">DEVELOPER NOTES</div><h1>A foundation for assurance.</h1><p>What works today, and the boundary of this preview.</p></div></div><section className="panel"><h2>Start with a transaction</h2><p>Open <Link href="/rehearse">Rehearse</Link>, choose mainnet or testnet, then enter a target address, ETH value and calldata. An executor and block number are optional. JSON imports use integer wei values; Safe exports support one transaction at a time.</p><h2>Live connections, explicit demo results</h2><p>Network status reads the actual chain ID and latest block every 30 seconds while the page is visible. Wallets connect through EIP-1193. Connecting never signs a message or sends a transaction. Disconnect ends the app session; manage site permissions inside your wallet to revoke access there.</p><p>The EVM execution engine is not connected. Every result is a fixed demo scenario, regardless of the entered payload. The locally computed Keccak-256 hash commits to the serialized input; it is not an on-chain transaction hash.</p><h2>Networks</h2><div className="docs-networks"><div><h3>Robinhood Chain</h3><p>Chain ID: 4663 · Gas: ETH</p><a href="https://robinhoodchain.blockscout.com" target="_blank" rel="noreferrer">Mainnet explorer ↗</a></div><div><h3>Robinhood Chain Testnet</h3><p>Chain ID: 46630 · Gas: ETH</p><a href="https://explorer.testnet.chain.robinhood.com" target="_blank" rel="noreferrer">Testnet explorer ↗</a></div></div><p>Network definitions were verified against the <a href="https://docs.robinhood.com/chain/connecting/" target="_blank" rel="noreferrer">official Robinhood Chain documentation</a> on September 22, 2026.</p><h2>Evidence contract</h2><p>The frontend models chain and block provenance, transaction payload hash, pre-state and post-state commitments, execution results, findings, timestamp and attestation. Demo evidence leaves unavailable fields explicitly empty and is downloadable as JSON.</p><h2>Connect a future engine</h2><p>Implement the typed RehearsalClient interface in the dedicated EVM rehearsal module. Validate responses at the API boundary and preserve chain and block provenance. Live mode must never fall back to a successful demo response on failure.</p><pre>{`interface RehearsalRequest {\n  chainId: number\n  from?: Address\n  to: Address\n  value: string // integer wei\n  data: Hex\n  blockNumber?: bigint\n}`}</pre><p>The engine is responsible for execution fidelity and attestations. The frontend does not certify the safety of a transaction.</p></section></div>; }
